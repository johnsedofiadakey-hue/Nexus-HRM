{
  "name": "nexus-hrm",
  "version": "5.0.0",
  "description": "Nexus HRM — Production-Ready Human Resource Management System",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "build": "cd server && npm run build && cd ../client && npm run build",
    "setup": "cd server && npm run setup",
    "db:migrate": "cd server && npx prisma migrate deploy",
    "db:migrate:dev": "cd server && npx prisma migrate dev",
    "db:studio": "cd server && npx prisma studio",
    "lint:server": "cd server && npx tsc --noEmit",
    "lint:client": "cd client && npx tsc --noEmit"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
