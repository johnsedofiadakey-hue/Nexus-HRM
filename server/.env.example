# ──────────────────────────────────────────────────────────
# Nexus HRM v2.0 — Environment Configuration
# Copy this file to .env and fill in your values
# ──────────────────────────────────────────────────────────

# PostgreSQL Database
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/nexus_hrm"

# Security — REQUIRED: minimum 32-character random string
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="replace-with-64-char-random-hex-string-never-share-this"

# Server
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL="http://localhost:3000"

# API Base URL (used for avatar/file URLs in production)
API_BASE_URL="https://api.yourdomain.com"

# Dev portal bypass key (keep very secret in production)
DEV_MASTER_KEY="your-dev-master-key"

# ──── Email (SMTP) ────────────────────────────────────────
# Option A: Gmail (use App Password, not your login password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@yourcompany.com

# Option B: SendGrid
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=SG.xxxxxxxxxxxxxxxxxx
# SMTP_FROM=noreply@yourcompany.com

# Option C: Resend
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=465
# SMTP_USER=resend
# SMTP_PASS=re_xxxxxxxxxxxxxxxxxx
# SMTP_FROM=noreply@yourdomain.com
